# Routing vers l'API REST de ColocMatching
coloc_matching.rest:
    resource: "@RestBundle/Resources/config/routing.yml"
    prefix: /rest

    
# Routing vers l'administration de ColocMatching
coloc_matching.admin:
    resource: "@AdminBundle/Resources/config/routing.yml"
    prefix: /admin
