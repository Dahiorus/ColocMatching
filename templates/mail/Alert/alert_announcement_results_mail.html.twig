{% extends 'mail/layout_base_mail.html.twig' %}

{% block content %}
    <p>{{- 'mail.body.alert.announcement'|trans({ '%alert%': alert }) -}}</p>
    <p>{{- 'mail.body.alert.count'|trans({ '%count%': count }) -}}</p>
    {% for result in results %}
        {% include 'mail/Alert/alert_announcement.html.twig' with { 'announcement': result } only %}
    {% endfor %}
    <br/>
{% endblock content %}