{% extends 'mail/layout_base_mail.html.twig' %}

{% block content %}
    {% set recipientName = from.getDisplayName() %}
    {% set link = url('coloc_matching.group_url', { 'id': id }) %}

    <p>
        {{- 'mail.body.invitation.answer.invitable.group'|trans({
            '%recipientName%': recipientName,
            '%link%': link
        })|raw|nl2br -}}
    </p>
    <p>{{- 'mail.body.url_problem'|trans({ '%link%': link })|nl2br -}}</p>
{% endblock content %}
