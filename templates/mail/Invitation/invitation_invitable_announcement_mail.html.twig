{% extends 'mail/layout_base_mail.html.twig' %}

{% block content %}
    {% set fromName = from.getDisplayName() %}
    {% set link = absolute_url(path('coloc_matching.announcement_url', { 'id': id })) %}

    <p>
        {{- 'mail.body.invitation.invitable.announcement'|trans({
            '%name%': fromName,
            '%link%': link
        })|raw -}}
    </p>
    {% if message is not empty %}
        <blockquote>{{- message|nl2br -}}</blockquote>
    {% endif %}
    <p>{{- 'mail.body.url_problem'|trans({ '%link%': link })|nl2br -}}</p>
{% endblock content %}
