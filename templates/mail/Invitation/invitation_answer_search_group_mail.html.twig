{% extends 'mail/layout_base_mail.html.twig' %}

{% block content %}
    {% set creatorName = from.getDisplayName() %}
    {% set link = url('coloc_matching.group_url', { 'id': id }) %}

    <p>
        {{- 'mail.body.invitation.answer.search.group'|trans({
            '%creatorName%': creatorName,
            '%link%': link
        })|raw|nl2br -}}
    </p>
    <p>{{- 'mail.body.url_problem'|trans({ '%link%': link })|nl2br -}}</p>
{% endblock content %}
