{% block body %}
    {% set recipientName = recipient.firstName ~ ' ' ~ recipient.lastName %}
    {% set fromName = from.firstName ~ ' ' ~ from.lastName %}

    <p>{{- 'mail.body.hello'|trans({ '%name%': recipientName }) -}}</p>
    {{- messageKey|trans({
        '%name%': fromName,
        '%message%': message,
        '%link%': link
    })|raw -}}
    <p>{{- 'mail.body.url_problem'|trans({ '%link%': link })|raw -}}</p>
    <p>{{- 'mail.body.footer'|trans|raw -}}</p>
{% endblock body %}

{% block signature %}
    {# TODO add a signature #}
{% endblock signature %}